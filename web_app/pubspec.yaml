name: web_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_inset_box_shadow: ^1.0.8 #Replaces default Boxdecoration and Shadow needs to be imported as an allias to avoid conflict with default
  flutter_svg: ^1.1.6
  http: ^1.4.0
  web: ^1.1.1
  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/name.svg
    - assets/background.png
    - assets/images.png
